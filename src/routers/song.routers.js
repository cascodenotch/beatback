const { Router } = require("express");
const router = Router();
const songCtrl = require("../controller/song.controller");

// router.get("/mis-canciones", songCtrl.getRecentlyPlayed);
// router.get("/recomendaciones", songCtrl.getRecommendations);

module.exports = router; 